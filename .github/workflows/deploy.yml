name: Deploy to Server

on:
  push:
    branches:
      - main

jobs:
  deploy:
    # 使用自托管的 runner，在服务器本地运行，无需 SSH
    runs-on: self-hosted

    steps:
      - name: 检出代码
        uses: actions/checkout@v4

      - name: 执行部署脚本
        run: |
          cd ${{ github.workspace }}
          echo "🔄 代码已更新到最新版本"
          echo "🚀 执行部署脚本..."
          chmod +x deploy.sh
          ./deploy.sh
          echo "✅ 部署完成！应用运行在: http://localhost:4000"

      - name: 部署状态通知
        if: always()
        run: |
          if [ ${{ job.status }} == 'success' ]; then
            echo "✅ 部署成功！"
          else
            echo "❌ 部署失败，请检查日志"
          fi
